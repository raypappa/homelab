---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bookworm-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi
  storageClassName: ceph-filesystem
